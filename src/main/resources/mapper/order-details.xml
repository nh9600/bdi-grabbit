<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
    PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
    "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.grabit.bdi.mapper.OrderDetailsMapper">

	<insert id="insertOrderDetails" parameterType="orderDetails">
		insert into order_details(oi_num, mi_num, od_quantity, mi_price)
		values(#{oiNum},#{miNum},#{odQuantity},#{miPrice})
	</insert>
	
</mapper>